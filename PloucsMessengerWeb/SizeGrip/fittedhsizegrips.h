#ifndef FITTEDHSIZEGRIPS_H
#define FITTEDHSIZEGRIPS_H

#include <QWidget>
#include "hsizegrip.h"

class FittedHSizeGrips : public QWidget
{
    Q_OBJECT
public:
    explicit FittedHSizeGrips(QWidget *parent = 0);
    ~FittedHSizeGrips();

signals:

public slots:
};

#endif // FITTEDHSIZEGRIPS_H
